#include "avltree.hpp"

auto main() -> int {
    // auto tree = AVLTree([](const int &left, const int &right) {
    //     if (left > right) {
    //         return 1;
    //     }
    //     if (left < right) {
    //         return -1;
    //     }
    //     return 0;
    // });
    //
    // for (int i = 100; i > 0; --i) {
    //     tree.insert(std::move(i));
    // }
    // tree.insert(std::move(0));
    // tree.in_order_print();
}
